const Game = { subscribers: 0, revenue: 0, videoViews: 0, videoCount: 0, createVideo: function() { // TODO: Implement createVideo function }, advertise: function() { // TODO: Implement advertise function }, updateStats: function() { document.getElementById('subscribers').textContent = this.subscribers; document.getElementById('revenue').textContent = `$${this.revenue}`; }, start: function() { setInterval(() => { this.videoViews += Math.floor(Math.random() * (500 - 100 + 1) + 100); this.subscribers += Math.floor(Math.random() * (10 - 1 + 1) + 1); this.revenue += this.subscribers * 0.01; this.updateStats(); }, 1000); } }; document.getElementById('create-video').addEventListener('click', () => { Game.createVideo(); }); document.getElementById('advertise').addEventListener('click', () => { Game.advertise(); }); Game.start();
